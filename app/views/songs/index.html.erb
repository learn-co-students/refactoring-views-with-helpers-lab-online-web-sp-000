<ul>
<% @songs.each do |song| %>
  <li>
  Artist: <%= link_to display_artist(song) %><br>
  Song: <%= link_to song.title, song_path(song)%><br>
  </li>

<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
