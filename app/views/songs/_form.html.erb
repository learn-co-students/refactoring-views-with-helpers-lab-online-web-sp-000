<%= form_for @song  do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %><br>
    <%= f.fields_for :artist do |a|%>
      <%= a.label "Artist Name" %>
      <%= a.text_field :name %>
    <% end %>
    <br />
  
    <%= f.submit %>
  <% end %>
  